# Publish to npm and the MCP Registry, both by GitHub OIDC. See issue #60 and
# google-workspace-mcp ADR-105 for the reasoning.
#
# THE FILENAME IS LOAD-BEARING, and is why a file that publishes two channels is
# still called npm-publish.yml. npmjs.com registers a trusted publisher against a
# repository AND a workflow filename; this file is registered as `npm-publish.yml`.
# Rename or move it and npm stops recognising the identity, with nothing in this
# repo to explain why.
#
# There is deliberately no NPM_TOKEN, no NODE_AUTH_TOKEN, and no `registry-url:` on
# setup-node — that input exists to write a token into .npmrc, which is what this
# replaces. OIDC has no secret to expire.
#
# The two channels differ in how the identity is granted. npm matches a publisher
# the package owner registered on npmjs.com. The MCP Registry reads the
# `repository_owner` claim out of the OIDC token and grants `io.github.<owner>/*`
# from that alone — so it needs no registry-side configuration, and equally offers
# no registry-side protection if this repository's owner ever changes.
name: Publish to npm and the MCP Registry

on:
  push:
    tags: ['v*']

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write   # mints the OIDC token npm verifies. Without it, publish is unauthenticated.
    steps:
      - uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'

      # Node 24 ships an npm 11.x that is usually new enough, and Node 22 shipped
      # npm 10.x which is not. Neither is a promise: the bundled npm moves with the
      # Node patch the runner happens to have. An npm below the 11.5.1 floor does
      # not report a missing feature — it looks for a token, finds none, and fails
      # with a 401 that says nothing about OIDC. Carrying the floor in the install
      # spec makes an unsatisfiable floor fail loudly right here.
      - name: Upgrade npm past the trusted-publishing floor
        run: |
          npm install -g 'npm@>=11.5.1'
          npm --version

      - name: Install dependencies
        run: npm ci

      # The tag is not evidence that anything was checked. Run the gates against
      # the commit actually being published, in a clean checkout.
      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      # A PRE-RELEASE must not become `latest`. `npm publish` with no --tag
      # publishes as `latest`, which every `npm install` and every `^x.y.z` range
      # picks up. Semver defines a pre-release as anything after `-`, so match on
      # that rather than an allowlist of names — an allowlist publishes the
      # pre-release spellings it forgot (`-pre`, `-next`, `-canary`) as `latest`.
      - name: Determine npm dist-tag
        id: npm-tag
        run: |
          VERSION=$(node -p "require('./package.json').version")
          case "$VERSION" in
            *-*)
              PRE="${VERSION#*-}"
              PRE="${PRE%%.*}"
              # npm rejects a dist-tag that parses as a semver range, which a
              # bare numeric identifier (v1.0.0-1) would.
              case "$PRE" in
                [0-9]*) PRE=prerelease ;;
              esac
              echo "tag=$PRE" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "tag=latest" >> "$GITHUB_OUTPUT"
              ;;
          esac
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      # The tag, package.json, server.json, and the mcpb manifest must agree on
      # one version, and server.json must point at the npm package this repo
      # publishes. One script owns that invariant; every publish path calls it.
      - name: Check the release identity
        run: node scripts/check-publish-identity.cjs "$GITHUB_REF_NAME"

      # An npm version is permanent, so a re-run of this job on an already-published
      # version must be a no-op rather than a red X on a release that actually
      # succeeded. The pre-check compares the served version rather than trusting
      # `npm view`'s exit code, and a failed publish re-checks the registry before
      # turning red — otherwise a transient registry error during the pre-check
      # turns a no-op re-run into a publish conflict.
      - name: Publish to npm
        run: |
          PKG=$(node -p "require('./package.json').name")
          VERSION="${{ steps.npm-tag.outputs.version }}"
          if [ "$(npm view "$PKG@$VERSION" version 2>/dev/null)" = "$VERSION" ]; then
            echo "$PKG@$VERSION is already on the registry — nothing to publish."
            exit 0
          fi
          if ! npm publish --provenance --access public --tag "${{ steps.npm-tag.outputs.tag }}"; then
            if [ "$(npm view "$PKG@$VERSION" version 2>/dev/null)" = "$VERSION" ]; then
              echo "$PKG@$VERSION is already on the registry — the publish conflict is a no-op."
              exit 0
            fi
            exit 1
          fi

      # A failed publish is easy to miss: it lives in a different workflow from
      # the tag push, and the .mcpb release succeeds beside it, so the release
      # looks done. Assert the registry actually serves the tagged version
      # rather than trusting the step above not to quietly no-op.
      - name: Verify the registry serves this version
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG=$(node -p 'require("./package.json").name')
          for i in 1 2 3 4 5; do
            PUBLISHED=$(npm view "$PKG@$TAG" version 2>/dev/null || true)
            if [ "$PUBLISHED" = "$TAG" ]; then
              echo "npm serves $PKG@$TAG"
              exit 0
            fi
            echo "registry has not caught up (attempt $i/5), waiting..."
            sleep 10
          done
          echo "FATAL: npm does not serve $PKG@$TAG after a reported-successful publish"
          exit 1

  mcp-registry:
    name: Publish to the MCP Registry
    runs-on: ubuntu-latest
    # server.json advertises the npm package AT THIS VERSION, so publishing the
    # registry entry first would point people at a tarball that does not exist
    # yet. `needs` is the ordering; it is not decoration.
    needs: publish
    permissions:
      contents: read
      id-token: write   # same OIDC token as the npm job; mcp-publisher reads it from the runner
    steps:
      - uses: actions/checkout@v7

      # Pinned deliberately. This runs on tag push, where a surprise upstream
      # change breaks a release at the worst possible moment and there is no green
      # build to compare against. Bump it on purpose, not by drifting onto `latest`.
      - name: Install mcp-publisher
        env:
          MCP_PUBLISHER_VERSION: '1.8.1'
        run: |
          curl -fsSL -o mcp-publisher.tar.gz \
            "https://github.com/modelcontextprotocol/registry/releases/download/v${MCP_PUBLISHER_VERSION}/mcp-publisher_linux_amd64.tar.gz"
          tar -xzf mcp-publisher.tar.gz mcp-publisher
          ./mcp-publisher --version

      # Same script as the npm job — server.json carries the version twice and
      # names the npm tarball this entry points at, and version-sync writes only
      # what it knows about. A mismatch here publishes a registry entry naming
      # the wrong tarball, which no npm-side checking would catch.
      - name: Check the release identity
        run: node scripts/check-publish-identity.cjs "$GITHUB_REF_NAME"

      - name: Authenticate by GitHub OIDC
        # No secret. The registry validates the token's issuer and audience, then
        # grants io.github.<repository_owner>/* from the `repository_owner` claim.
        run: ./mcp-publisher login github-oidc

      # Same reasoning as the npm step: a re-run on an already-published version
      # must be a no-op rather than a red X on a release that already succeeded.
      # The script filters the registry search by exact server name and treats a
      # failed probe as "unknown, publish anyway".
      - name: Publish to the MCP Registry
        run: MCP_PUBLISHER=./mcp-publisher bash scripts/mcp-registry-publish.sh
